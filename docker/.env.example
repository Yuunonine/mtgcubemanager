# MTG Cubes Application Environment Variables

# Server Configuration
PORT=3001
NODE_ENV=production

# Database Configuration
# SQLite database will be stored in /app/server/data/cubes.db inside container
# This directory is persisted via Docker volume

# Optional: API Configuration
# SCRYFALL_API_BASE_URL=https://api.scryfall.com

# Optional: Logging Configuration
# LOG_LEVEL=info
# LOG_FILE=/app/logs/app.log

# Optional: CORS Configuration (if needed)
# CORS_ORIGIN=*